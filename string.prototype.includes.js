const sent = 'I will find my Purpose not on programming but in Hacking and in CTFs';
const word = 'Purpose';
console.log(`the word"${word}" ${sent.includes(word) ? 'is': 'is not'} in the sentence`);